# Nusxa oling:  cp .env.example .env

# Dev: /api va /media so'rovlari shu Django manziliga proxy qilinadi
API_PROXY_TARGET=http://127.0.0.1:8000

# Backend boshqa domenda bo'lsa (masalan https://api.example.com). Bo'sh — nisbiy /api
VITE_API_URL=
